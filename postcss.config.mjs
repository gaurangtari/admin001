const config = {
  plugins: ["@tailwindcss/postcss"],
  theme: {
    extend: {
      colors: {
        primary: "#0a192f",
        secondary: "#112240",
        accent: "#64ffda",
      },
    },
  },
};

export default config;
